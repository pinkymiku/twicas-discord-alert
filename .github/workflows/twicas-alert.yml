name: Discord Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: |
          curl -X POST \
          -H "Content-Type: application/json" \
          -d '{"content":"Discord通知テスト成功"}' \
          "${{ secrets.DISCORD_WEBHOOK }}"
